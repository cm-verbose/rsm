# rsm 

**rsm** stands for "ressource manager" and is used to read and manipulate data in certain types of files. 

## Testing

- Run tests with `cargo test`
- Run mutation testing with `cargo mutants`
- Run fuzzing with `cargo +nightly fuzz <fuzz_target>`
- Inspect coverage with `cargo llvm-cov`
